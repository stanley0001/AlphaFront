export interface Account{
    accountBalance: Number,
    accountId: Number,
    amount: Number,
    applicationId: Number,
    customerId: string,
    dueDate: Date,
    otherRef: string,
    payableAmount: Number,
    startDate: Date,
    status: string
}